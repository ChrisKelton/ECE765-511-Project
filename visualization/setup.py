#!/usr/bin/env python
from distutils.core import setup

setup(
    name="visualization",
    version="0.0",
    description="Visualization of data and models via plots, graphs, etc...",
    packages=["visualization"],
)